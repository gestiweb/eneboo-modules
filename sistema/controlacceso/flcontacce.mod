<!DOCTYPE MODULE>
<MODULE>
	<name>flcontacce</name>
	<alias>QT_TRANSLATE_NOOP("FLWidgetApplication","Control de acceso")</alias>
	<area>sys</area>
	<areaname>QT_TRANSLATE_NOOP("FLWidgetApplication","Sistema")</areaname>
	<version>2.3</version>
	<icon>flcontacce.xpm</icon>
	<flversion>2.3</flversion>
	<description>Este módulo permite restringir el acceso a determinadas funcionalidades de FacturaLUX a usuarios o grupos de usuarios.<br/> El funcionamiento de este módulo se basa en las listas de control de acceso (ACLs), en las que, a un determinado elemento (un campo, una tabla, un botón, etc.), se asocia una lista en la que se define el tipo de acceso (ninguno, sólo lectura, completo, etc.) que tienen los distintos usuarios de la aplicación.
	</description>
</MODULE>